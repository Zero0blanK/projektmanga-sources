{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/Zero0blanK/projektmanga-sources/main/schema/manifest.schema.json",
  "title": "ProjektManga source manifest",
  "description": "src/<source_key>/manifest.json — everything a source declares by hand. Fields the build can derive (entry_url, icon_url, version_code, api_version, isNSFW, bundle hash) are deliberately absent. build.mjs enforces this schema and a few rules JSON Schema cannot express; see docs/repository-format.md.",
  "type": "object",
  "additionalProperties": false,
  "required": ["source_key", "name", "lang", "version", "base_url", "allowed_hosts", "content_rating"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "source_key": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "Stable identity. Must equal the source's directory name — the bundle is published as dist/<source_key>.js. Never reuse a key for a different site."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Display name shown in the install listing."
    },
    "lang": {
      "type": "string",
      "pattern": "^([a-z]{2,3}(-[A-Za-z0-9]{2,8})*|all)$",
      "description": "BCP-47 code (en, pt-BR, zh-Hant), or \"all\" for a source that is language-agnostic."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d{1,3}\\.\\d{1,3}\\.\\d{1,3}$",
      "description": "major.minor.patch, each part 0-999. Bump whenever the source's code changes; the build derives version_code from it."
    },
    "description": {
      "type": "string",
      "maxLength": 200,
      "description": "One line for the install listing."
    },
    "base_url": {
      "type": "string",
      "format": "uri",
      "pattern": "^https://",
      "description": "Where the source's requests go. For an API-backed source this is the API origin, not the site."
    },
    "homepage": {
      "type": "string",
      "format": "uri",
      "pattern": "^https://",
      "description": "Where a human should be sent to see the site, when that differs from base_url. Defaults to base_url."
    },
    "allowed_hosts": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?(\\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+$"
      },
      "description": "Every host the source may reach, including hosts reached only via a redirect. Bare lowercase hostnames: no scheme, port, path or wildcard. Matching is exact or by suffix, so \"example.com\" also permits \"cdn.example.com\". The build rejects a list that does not cover base_url."
    },
    "content_rating": {
      "enum": ["safe", "mixed", "nsfw"],
      "description": "safe = no adult content; mixed = general catalogue that also carries adult titles; nsfw = adult site. Only \"nsfw\" sets isNSFW in the index."
    },
    "draft": {
      "type": "boolean",
      "description": "Build the source but keep it out of index.json. Requires draft_reason."
    },
    "draft_reason": {
      "type": "string",
      "minLength": 1,
      "description": "Why this source is held back, and what would have to change to publish it."
    }
  },
  "dependencies": {
    "draft": ["draft_reason"]
  }
}
